@startuml
start
:Execute Find command Ψ;
if (Command is find?) then ([yes])
  :Read selected trainer (if any);
  :Model.updateFilteredPersonList(predicate);
  :Re-apply selected trainer (if any);
else ([no])
  :Model.updateFilteredTrainerList(predicate)
  or updateFilteredClientList(predicate);
endif
:UI observes filtered lists;
:Lists in UI update;
stop
@enduml
